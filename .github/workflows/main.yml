name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: a339b39e-0cc4-4d95-96d5-35b54fa6aa91
          heroku_app_name: "hihiworld" #Must be unique in Heroku
          heroku_email: "fengv1976@gmail.com"
